{
  "version": 2,
  "name": "portfolio",
  "outputDirectory": "dist",
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "routes": [{ "src": "/(.*)", "dest": "/esbuild.js" }],
  "builds": [{ "src": "/esbuild.js", "use": "@vercel/node" }]
}